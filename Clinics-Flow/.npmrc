# Disable npm workspace hoisting to fix Next.js build issues
# Next.js needs to find its package.json in the app directory

# Don't hoist common dependencies
shamefully-hoist=false
public-hoist-pattern[]=*
public-hoist-pattern[]=@next/**
public-hoist-pattern[]=next
